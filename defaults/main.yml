---
container_storage: /opt/containers/storage
podman_packages:
  - crun
  - fuse-overlayfs
  - git
  - podman
  - podman-plugins
  - shadow-utils
  - slirp4netns
podman_user: podman
podman_group: podman
podman_runtime: crun
runroot: "{{ podman_user_home }}/containers/runroot"
